name: Build
on:
    pull_request:
    workflow_dispatch:

concurrency:
    group: "${{ github.workflow }}-${{ github.ref }}"
    cancel-in-progress: true

jobs:
    action_down:
        runs-on: ubuntu-latest
        if: always()
        steps:
            - name: First step
              continue-on-error: true
              run: ./De
#adsadsadsadasd